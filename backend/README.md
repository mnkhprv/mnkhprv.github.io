Pinpoint backend stub (Express)
Requirements: Node 18+
Install:
  cd backend
  npm install
Run:
  npm start
This stub exposes:
  GET /places?q=coffee&lat=47.9&lng=106.9
  GET /places/:id
  GET /itineraries?userId=...
  POST /itineraries

It uses mock data in data/mock_places.json
